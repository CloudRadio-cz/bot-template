/**
 * Color configuration for the Discord bot.
 *
 * Defines the color palette used throughout the bot for embeds,
 * messages, and other visual elements. Colors are specified
 * in hexadecimal format compatible with Discord's color system.
 *
 * @type {Object} Color configuration object
 * @property {number} main - Main theme color (red)
 * @property {number} success - Success state color (green)
 * @property {number} error - Error state color (red)
 * @property {number} warn - Warning state color (orange)
 */
export const colors = {
	main: 0xcc0000, // Main color - Red
	success: 0x00cc00, // Success color - Green
	error: 0xcc0000, // Error color - Red
	warn: 0xff9200, // Warning color - Orange
};
