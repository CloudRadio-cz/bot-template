/**
 * Emoji configuration for the Discord bot.
 *
 * Defines the emoji collection used throughout the bot for
 * visual feedback, status indicators, and message enhancement.
 * These emojis provide quick visual cues for different states and actions.
 *
 * @type {Object} Emoji configuration object
 * @property {string} success - Success state emoji (green checkmark)
 * @property {string} error - Error state emoji (red X)
 */
export const emojis = {
	success: "✅", // Green checkmark for success states
	error: "❌", // Red X for error states
};
